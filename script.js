const videoElememt = document.getElementById('video');
const button = document.getElementById('button');

// Promp the user to select media stream, pass to video element, then play
async function selectMediaStream() {
    try {

    } catch (error) {
        // catch error here
    }
}